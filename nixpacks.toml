# Nixpacks detecta requirements.txt en la raíz automáticamente.
# Solo necesitamos definir el comando de inicio.

[start]
cmd = "gunicorn --chdir app/backend app:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120"
